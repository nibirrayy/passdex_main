export default function VaultEntries(props) {
    return (
        <div className="rounded-full bg-gray-500 h-24 w-24 m-3.5 flex items-center justify-center">
            {props.name}
        </div>
    );
}